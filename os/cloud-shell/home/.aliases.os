#!/usr/bin/env bash
# GCP Cloud Shell-specific aliases
# This file is sourced after .aliases (shared) and before .aliases.local
# Place Cloud Shell-specific command shortcuts here

# Google Cloud SDK aliases
# alias gc="gcloud"
# alias kc="kubectl"

# Project shortcuts
# alias setproj="gcloud config set project"
# alias currentproj="gcloud config get-value project"

# Example: Quick SSH to compute instance
# alias ssh-vm="gcloud compute ssh"
